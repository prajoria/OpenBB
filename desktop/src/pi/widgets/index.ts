/**
 * Side-effectful barrel — importing this module registers every widget
 * in the SDK registry. The canvas + preview + tests all import here so
 * a widget dropped into ``./widgets/`` and added to the list below
 * becomes available everywhere in one step.
 *
 * Widget authors: add your ``import "./your-widget";`` line here.
 * Do NOT export values from this file; it's registration-only.
 */

import "./hello";
