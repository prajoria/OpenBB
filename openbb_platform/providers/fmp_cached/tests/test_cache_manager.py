"""Legacy scaffolding — module intentionally empty (see #752)."""

import pytest

pytest.skip('Legacy scaffolding from PR #12; imports symbols that no longer exist. See #752 (kept as placeholder to preserve git history; contents removed to prevent collection failures).', allow_module_level=True)
