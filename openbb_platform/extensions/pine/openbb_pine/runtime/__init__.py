"""``openbb_pine.runtime`` -- D2 runtime adapter layer.

Houses provider selection, FMP/BYO data providers, error mapping helpers,
and the eventual ``PineExecutor`` that wraps PyneCore's ``ScriptRunner``.
Module file map locked by D2 Appendix C.
"""
