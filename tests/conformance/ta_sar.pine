// Conformance fixture for ta.sar (S-bead OpenBBTechnical-0e9.5.39, Wave 5B-3).
//
// Author-derivative posture per PRD §7.3: reference output computed
// deterministically by running this script's Parabolic SAR formula
// against a hard-coded synthetic OHLC series (see ta_sar.csv `high`/`low`).
// Uses Wilder's defaults: start=0.02 inc=0.02 max=0.2. Bar 0 is NA
// (SAR seeds on bar 1 using bar-0 vs bar-1 highs to pick initial trend).
//@version=6
indicator("smoke_ta_sar", overlay=true)
plot(ta.sar(0.02, 0.02, 0.2), title="sar")
