# Portfolio App — Requirements
#
# Install into the .venv_win environment (from-source OpenBB):
#   .venv_win\Scripts\pip install -r portfolio_app\requirements.txt
#
# NOTE: OpenBB Platform is NOT listed here — it is installed from
# source in .venv_win via the project-level dev setup.
# .venv_openbb is a separate env for running pre-built OpenBB packages.

# --- Portfolio app dependencies ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pymysql>=1.1.0
httpx>=0.27.0
python-dotenv>=1.0.0
